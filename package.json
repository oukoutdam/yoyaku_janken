{
  "name": "yoyaku_janken",
  "version": "1.0.0",
  "description": "An app where we can play rock papers scissors at separate times",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"nodemon index.js\" \"npx @tailwindcss/cli -i ./global.css -o ./public/output.css --watch\"",
    "build": "npm install && npx @tailwindcss/cli -i ./global.css -o ./public/output.css --minify",
    "start": "node index.js"
  },
  "author": "Ouk Outdam",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "mongodb": "^6.12.0",
    "mongoose": "^8.9.5",
    "tailwindcss": "^4.0.0"
  },
  "type": "module",
  "devDependencies": {
    "daisyui": "^5.0.0-beta.2",
    "nodemon": "^3.1.9"
  }
}
